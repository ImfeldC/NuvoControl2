<UserControl x:Class="NuvoControl.Client.Viewer.Controls.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:commands="clr-namespace:NuvoControl.Client.Viewer.Commands"
    MinWidth="300" MinHeight="300" Visibility="{Binding Path=Visibility1}" Focusable="True">
    <UserControl.ContextMenu>
        <ContextMenu>
            <MenuItem Command="commands:CustomCommands.BrowseUp"></MenuItem>
            <MenuItem Command="commands:CustomCommands.BrowseDown"></MenuItem>
            <MenuItem Command="commands:CustomCommands.BrowsePrevious"></MenuItem>
            <MenuItem Command="commands:CustomCommands.BrowseNext"></MenuItem>
        </ContextMenu>
    </UserControl.ContextMenu>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Image Grid.Row="2" Margin="5" Name="_imageBuilding" Stretch="Uniform" Source="{Binding Path=MainImage}" Focusable="True"
               MouseLeftButtonUp="_imageBuilding_OnMouseLeftButtonUp"/>
    </Grid>
</UserControl>
